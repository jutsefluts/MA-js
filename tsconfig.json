{
  "compilerOptions": {
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,                     // Allows JavaScript files to be compiled.
    "skipLibCheck": true,                // Skip type checking of all declaration files (helps improve compilation speed).
    "strict": true,                      // Enables strict type-checking options (recommended for better type safety).
    "noEmit": true,                      // Prevents emitting compiled files (useful for a Next.js project).
    "esModuleInterop": true,             // Enables interoperation between CommonJS and ES Modules.
    "module": "esnext",                  // Set the module system to ESNext (recommended for modern projects).
    "moduleResolution": "bundler",       // Module resolution strategy based on the bundler.
    "resolveJsonModule": true,           // Allows importing JSON modules as objects.
    "isolatedModules": true,             // Ensures each file is treated as a separate module.
    "jsx": "preserve",                   // Keeps JSX in the output to be handled by Next.js.
    "incremental": true,                 // Enable faster builds with incremental compilation.
    "plugins": [
      {
        "name": "next"                   // Plugin for Next.js support.
      }
    ],
    "paths": {
      "@/*": ["./*"]                     // Path alias to shorten import paths for project files.
    }
  },
  "include": [
    "next-env.d.ts",                     // Next.js TypeScript environment declaration.
    "**/*.ts",                           // Include all TypeScript files.
    "**/*.tsx",                          // Include all TSX files (React components).
    ".next/types/**/*.ts"                // Include types generated by Next.js.
  ],
  "exclude": [
    "node_modules"                       // Exclude external dependencies.
  ]
}
